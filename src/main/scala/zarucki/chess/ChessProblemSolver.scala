package zarucki.chess

import zarucki.chess.entities.{Board, Piece}

object ChessProblemSolver {
	def solveNonThreatenProblem(boardWidth: Int, boardHeight: Int, chessPiecesToPlace: Seq[Piece]): Set[Board] = {
		Set()
	}
}
